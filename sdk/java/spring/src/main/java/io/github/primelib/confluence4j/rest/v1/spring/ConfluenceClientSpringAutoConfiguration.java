// WARNING: This file was generated by PrimeCodeGen. DO NOT EDIT.

package io.github.primelib.confluence4j.rest.v1.spring;

import io.github.primelib.confluence4j.rest.v1.ConfluenceClientFactory;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientAnalyticsApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientAnalyticsConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientAuditApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientAuditConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentAttachmentsApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentAttachmentsConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentBodyApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentBodyConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentChildrenAndDescendantsApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentChildrenAndDescendantsConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentCommentsApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentCommentsConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentLabelsApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentLabelsConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentMacroBodyApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentMacroBodyConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentPermissionsApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentPermissionsConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentPropertiesApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentPropertiesConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentRestrictionsApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentRestrictionsConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentStatesApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentStatesConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentVersionsApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentVersionsConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentWatchesApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientContentWatchesConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientDynamicModulesApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientDynamicModulesConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientExperimentalApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientExperimentalConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientGroupApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientGroupConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientInlineTasksApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientInlineTasksConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientLabelInfoApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientLabelInfoConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientLongRunningTaskApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientLongRunningTaskConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientRelationApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientRelationConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSearchApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSearchConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSettingsApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSettingsConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSpaceApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSpaceConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSpacePermissionsApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSpacePermissionsConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSpacePropertiesApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSpacePropertiesConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSpaceSettingsApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientSpaceSettingsConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientTemplateApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientTemplateConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientThemesApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientThemesConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientUserPropertiesApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientUserPropertiesConsumerApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientUsersApi;
import io.github.primelib.confluence4j.rest.v1.client.ConfluenceClientUsersConsumerApi;
import io.github.primelib.primecodegenlib.java.feign.common.api.AuthMethod;
import io.github.primelib.primecodegenlib.java.feign.common.auth.BearerAuthSpec;
import io.github.primelib.primecodegenlib.java.feign.common.auth.OAuth2ClientCredentialsAuthSpec;
import io.github.primelib.primecodegenlib.java.feign.common.auth.OAuth2UserCredentialsAuthSpec;
import io.micrometer.core.instrument.MeterRegistry;
import java.util.List;
import javax.annotation.processing.Generated;
import lombok.Data;
import lombok.extern.slf4j.Slf4j;
import org.springframework.boot.autoconfigure.AutoConfiguration;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Primary;

@AutoConfiguration
@EnableConfigurationProperties(ConfluenceClientSpringAutoConfiguration.Properties.class)
@ConditionalOnProperty(name = "confluence-client.url", matchIfMissing = false)
@Slf4j
@Generated(value = "io.github.primelib.primecodegen")
public class ConfluenceClientSpringAutoConfiguration {
    private final Properties properties;
    private final MeterRegistry meterRegistry;

    public ConfluenceClientSpringAutoConfiguration(Properties properties, MeterRegistry meterRegistry) {
        this.properties = properties;
        this.meterRegistry = meterRegistry;
    }

    @Bean
    @Primary
    public ConfluenceClientApi ConfluenceClientApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientConsumerApi ConfluenceClientConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    
    @Bean
    @Primary
    public ConfluenceClientAnalyticsApi ConfluenceClientAnalyticsApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientAnalyticsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientAnalyticsConsumerApi ConfluenceClientAnalyticsConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientAnalyticsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientAuditApi ConfluenceClientAuditApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientAuditApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientAuditConsumerApi ConfluenceClientAuditConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientAuditConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentApi ConfluenceClientContentApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentConsumerApi ConfluenceClientContentConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentAttachmentsApi ConfluenceClientContentAttachmentsApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentAttachmentsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentAttachmentsConsumerApi ConfluenceClientContentAttachmentsConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentAttachmentsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentChildrenAndDescendantsApi ConfluenceClientContentChildrenAndDescendantsApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentChildrenAndDescendantsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentChildrenAndDescendantsConsumerApi ConfluenceClientContentChildrenAndDescendantsConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentChildrenAndDescendantsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentMacroBodyApi ConfluenceClientContentMacroBodyApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentMacroBodyApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentMacroBodyConsumerApi ConfluenceClientContentMacroBodyConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentMacroBodyConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentBodyApi ConfluenceClientContentBodyApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentBodyApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentBodyConsumerApi ConfluenceClientContentBodyConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentBodyConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentCommentsApi ConfluenceClientContentCommentsApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentCommentsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentCommentsConsumerApi ConfluenceClientContentCommentsConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentCommentsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentLabelsApi ConfluenceClientContentLabelsApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentLabelsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentLabelsConsumerApi ConfluenceClientContentLabelsConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentLabelsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentPermissionsApi ConfluenceClientContentPermissionsApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentPermissionsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentPermissionsConsumerApi ConfluenceClientContentPermissionsConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentPermissionsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentPropertiesApi ConfluenceClientContentPropertiesApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentPropertiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentPropertiesConsumerApi ConfluenceClientContentPropertiesConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentPropertiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentRestrictionsApi ConfluenceClientContentRestrictionsApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentRestrictionsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentRestrictionsConsumerApi ConfluenceClientContentRestrictionsConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentRestrictionsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentStatesApi ConfluenceClientContentStatesApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentStatesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentStatesConsumerApi ConfluenceClientContentStatesConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentStatesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentVersionsApi ConfluenceClientContentVersionsApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentVersionsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentVersionsConsumerApi ConfluenceClientContentVersionsConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentVersionsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientContentWatchesApi ConfluenceClientContentWatchesApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentWatchesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientContentWatchesConsumerApi ConfluenceClientContentWatchesConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientContentWatchesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientDynamicModulesApi ConfluenceClientDynamicModulesApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientDynamicModulesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientDynamicModulesConsumerApi ConfluenceClientDynamicModulesConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientDynamicModulesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientExperimentalApi ConfluenceClientExperimentalApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientExperimentalApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientExperimentalConsumerApi ConfluenceClientExperimentalConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientExperimentalConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientGroupApi ConfluenceClientGroupApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientGroupApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientGroupConsumerApi ConfluenceClientGroupConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientGroupConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientInlineTasksApi ConfluenceClientInlineTasksApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientInlineTasksApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientInlineTasksConsumerApi ConfluenceClientInlineTasksConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientInlineTasksConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientLabelInfoApi ConfluenceClientLabelInfoApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientLabelInfoApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientLabelInfoConsumerApi ConfluenceClientLabelInfoConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientLabelInfoConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientLongRunningTaskApi ConfluenceClientLongRunningTaskApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientLongRunningTaskApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientLongRunningTaskConsumerApi ConfluenceClientLongRunningTaskConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientLongRunningTaskConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientRelationApi ConfluenceClientRelationApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientRelationApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientRelationConsumerApi ConfluenceClientRelationConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientRelationConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientSearchApi ConfluenceClientSearchApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSearchApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientSearchConsumerApi ConfluenceClientSearchConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSearchConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientSettingsApi ConfluenceClientSettingsApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSettingsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientSettingsConsumerApi ConfluenceClientSettingsConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSettingsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientSpaceApi ConfluenceClientSpaceApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSpaceApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientSpaceConsumerApi ConfluenceClientSpaceConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSpaceConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientSpacePermissionsApi ConfluenceClientSpacePermissionsApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSpacePermissionsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientSpacePermissionsConsumerApi ConfluenceClientSpacePermissionsConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSpacePermissionsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientSpacePropertiesApi ConfluenceClientSpacePropertiesApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSpacePropertiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientSpacePropertiesConsumerApi ConfluenceClientSpacePropertiesConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSpacePropertiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientSpaceSettingsApi ConfluenceClientSpaceSettingsApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSpaceSettingsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientSpaceSettingsConsumerApi ConfluenceClientSpaceSettingsConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientSpaceSettingsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientTemplateApi ConfluenceClientTemplateApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientTemplateApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientTemplateConsumerApi ConfluenceClientTemplateConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientTemplateConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientThemesApi ConfluenceClientThemesApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientThemesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientThemesConsumerApi ConfluenceClientThemesConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientThemesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientUserPropertiesApi ConfluenceClientUserPropertiesApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientUserPropertiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientUserPropertiesConsumerApi ConfluenceClientUserPropertiesConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientUserPropertiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public ConfluenceClientUsersApi ConfluenceClientUsersApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientUsersApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public ConfluenceClientUsersConsumerApi ConfluenceClientUsersConsumerApi() {
        return ConfluenceClientFactory.create(spec -> {
            spec.api(ConfluenceClientUsersConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    

    private List<AuthMethod> buildAuthSpecs(Properties props) {
        if (props.getAuth() == null || props.getAuth().getType() == null) {
            return null;
        }

        switch (props.getAuth().getType()) {
            case "oauth2-user":
                return List.of(new OAuth2UserCredentialsAuthSpec(auth -> {
                    auth.tokenEndpoint(props.getAuth().getTokenEndpoint());
                    auth.clientId(props.getAuth().getClientId());
                    auth.clientSecret(props.getAuth().getClientSecret());
                    auth.username(props.getAuth().getUsername());
                    auth.password(props.getAuth().getPassword());
                }));
            case "oauth2-client":
                return List.of(new OAuth2ClientCredentialsAuthSpec(auth -> {
                    auth.tokenEndpoint(props.getAuth().getTokenEndpoint());
                    auth.clientId(props.getAuth().getClientId());
                    auth.clientSecret(props.getAuth().getClientSecret());
                }));
            case "bearer":
                return List.of(new BearerAuthSpec(auth -> {
                    if (props.getAuth().getTokenPropertyKey() != null && !props.getAuth().getTokenPropertyKey().isEmpty()) {
                        auth.propertyKey(props.getAuth().getTokenPropertyKey());
                    }
                    if (props.getAuth().getTokenValueTemplate() != null && !props.getAuth().getTokenValueTemplate().isEmpty()) {
                        auth.propertyKey(props.getAuth().getTokenValueTemplate());
                    }
                    auth.propertyKey(props.getAuth().getToken());
                }));
            default:
                return null;
        }
    }

    @Data
    @ConfigurationProperties(prefix = "confluence-client")
    public static class Properties {
        private String url = "";
        private String backendName = "";
        private String logLevel = "";
        private Boolean insecure = false;
        private Auth auth;

        @Data
        public static class Auth {
            /**
             * Type of authentication.
             * Supported values: "oauth2-user", "oauth2-client", "bearer".
             */
            private String type;
            /**
             * Full token endpoint URL
             */
            private String tokenEndpoint;
            private String clientId;
            private String clientSecret;
            private String username;
            private String password;
            private String token;
            /**
             * Header key to pass the token in (used by BearerAuthSpec).
             */
            private String tokenPropertyKey;
            /**
             * Template to generate token value (used by BearerAuthSpec).
             */
            private String tokenValueTemplate;
        }
    }
}
